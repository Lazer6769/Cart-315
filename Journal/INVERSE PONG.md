# day1: 
## Came up with my idea for the game, the game will be the inverse of pong so instead of the paddles, borders and ball being white they're going to be the reverse of that and same goes for the background score board and court lines

### change their colors and going to change the speed of everything so it makes it faster instead of slower 

# day2: 
## changed the speed of all 3 only problem is that the CPU follows the ball instead of being unpredictable, as well as changing the score so instead of gaining points you lose points  

### goal for next day, add a score cap, make sure the code actually changes if possible make them both spin at random (CPU spins randomly, player presses a button to spin) 

### day3:
#### today I changed the the way the CPU moves, the ball player and CPU can spin at random 
#### the ball changes size at random 
#### the CPU moves in unpredictable way as well as the score giving the players negative points 
